# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# Flutter/Dart/Pub related
**/doc/api/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# signap_signals is a published library — do NOT commit the resolved lock
# (pub regenerates it; only apps pin it). The example/ (publish_to: none) also
# regenerates fresh in CI — see #1531 (removed its stale Dart-3.12.2 pin).
pubspec.lock
